# Updates with Luke

Luke wants updates through conversation, as he supplies and approves more details. Do not create scheduled reviews, reminders, background jobs or automatic content-refresh tasks.

The GitHub Pages workflow deploys a reviewed push; it is not a scheduled content-writing job.

Preserve all source references and unresolved details until Luke supplies confirmation. Keep work and education as the initial scope. Do not invent employment, qualifications, dates or current licence status. See updates.md for the content file format.

Before publishing, run `npm run build`, `npm run check` and `git diff --check`. Review the changed content and test affected interactions on desktop and mobile. Preserve unrelated changes. Commit only intended files and push normally to `main` at `https://github.com/auraofintelligence/Lukes-world-of-work-experience.git`.

After pushing, confirm that the remote commit matches, the exact commit's Pages workflow succeeded, and the live page, content file and generated assets are available. The site remains public under the Strange But True Public Source Licence.
